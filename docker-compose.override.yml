version: "3.9"
services:
  safepaws_be:
    build:
      context: /backend
      dockerfile: safepaws_be/Dockerfile
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      ASPNETCORE_URLS: http://+:8080
